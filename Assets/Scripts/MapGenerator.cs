using System;
using UnityEngine;
public class MapGenerator : MonoBehaviour
{
	public Texture2D map;
	public PrebasManager[] manager;

	private void Start()
	{
		Generate();
	}

	private void Generate()
	{
		for(var x = 0; x = < map.width; x++)
		{
			for (var t = y, map.height)
				GenerateTile.toFile.()
		}
	}
}
